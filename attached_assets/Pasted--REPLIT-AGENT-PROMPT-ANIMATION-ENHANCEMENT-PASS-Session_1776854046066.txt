━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
REPLIT AGENT PROMPT — ANIMATION ENHANCEMENT PASS
Session: 6 | Phase: 2 | Date: 2026-04-22
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You are building bpolytix.com — a professional SaaS/BPO marketing website.

BEFORE DOING ANYTHING ELSE, read and internalise these rules:

TECH STACK (do not deviate):
- Framework: Next.js 15 with App Router
- Styling: Tailwind CSS
- Animations: Framer Motion
- Icons: Lucide React ONLY — no other icon libraries
- Fonts: Syne (headings) + DM Sans (body) via next/font/google
- No other packages without asking first

COLOURS (exact hex — use only these, no others):
- Background:  #0D1B2A
- Surface:     #111F2E
- Accent:      #1B77F2  (ONE CTA button + key icons only)
- Highlight:   #00D4AA  (stats and hover states only)
- Text:        #F5F7FA
- Muted:       #8892A4
- Border:      #1E2D3D

COPY RULES:
- Do not change any existing copy
- Do not add, remove, or rewrite any text

OUTPUT RULES:
- Build ONLY what is in TODAY'S TASK
- Do not modify layout, copy, or page structure
- After completing, list every file changed and every effect added
- If you hit an error you cannot fix in 2 attempts, stop and describe it clearly

---

TODAY'S TASK: Strengthen existing Framer Motion animations and add a distinct border glow effect to all cards and interactive elements. The current animations are too subtle — make them unmistakably visible without breaking the linear.app dark aesthetic.

The Next.js project lives at: artifacts/bpolytix/

---

PART 1 — STRENGTHEN EXISTING REVEAL ANIMATIONS

Edit artifacts/bpolytix/components/Reveal.tsx

Change the current values to these stronger values:
- opacity: 0 → 1 (keep)
- y: 20 → 0 — INCREASE to y: 40 → 0
- duration: 0.6s — INCREASE to 0.7s
- ease: easeOut — CHANGE to [0.25, 0.1, 0.25, 1] (custom cubic bezier — more pronounced deceleration)
- Add: blur filter — filter: blur(4px) → blur(0px) — this makes the reveal feel cinematic and distinct

The updated Reveal motion.div initial/animate should be:
- initial: { opacity: 0, y: 40, filter: "blur(4px)" }
- animate (when in view): { opacity: 1, y: 0, filter: "blur(0px)" }
- transition: { duration: 0.7, ease: [0.25, 0.1, 0.25, 1], delay: (prop) }

---

PART 2 — STRONGER CARD HOVER LIFT

Edit artifacts/bpolytix/app/globals.css

Replace the existing .card-hover rule with:
```css
.card-hover {
  transition: transform 200ms ease, box-shadow 200ms ease;
}
.card-hover:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
}
```

---

PART 3 — BORDER GLOW ON CARDS AND INTERACTIVE ELEMENTS

This is the most important part. Add a glowing border effect that is visible on:
- Desktop: mouse hover
- Mobile: touch (use :focus-visible and :active as the trigger for touch devices)

Add these new CSS classes to artifacts/bpolytix/app/globals.css:

```css
/* Teal border glow — for service cards, pricing cards, FAQ cards */
.glow-border {
  border: 1px solid #1E2D3D;
  transition: border-color 250ms ease, box-shadow 250ms ease;
}
.glow-border:hover,
.glow-border:focus-visible,
.glow-border:active {
  border-color: rgba(0, 212, 170, 0.6);
  box-shadow:
    0 0 0 1px rgba(0, 212, 170, 0.15),
    0 0 20px rgba(0, 212, 170, 0.12),
    inset 0 0 20px rgba(0, 212, 170, 0.04);
  outline: none;
}

/* Blue border glow — for CTA buttons only */
.glow-border-blue {
  border: 1px solid transparent;
  transition: border-color 250ms ease, box-shadow 250ms ease;
}
.glow-border-blue:hover,
.glow-border-blue:focus-visible,
.glow-border-blue:active {
  border-color: rgba(27, 119, 242, 0.6);
  box-shadow:
    0 0 0 1px rgba(27, 119, 242, 0.15),
    0 0 24px rgba(27, 119, 242, 0.2),
    inset 0 0 20px rgba(27, 119, 242, 0.05);
  outline: none;
}
```

Then apply these classes to elements across all pages:

artifacts/bpolytix/app/services/page.tsx
- Add class "glow-border" to every service card/block wrapper div

artifacts/bpolytix/app/pricing/page.tsx
- Add class "glow-border" to every pricing plan card and every FAQ card
- Add class "glow-border-blue" to every primary CTA button (bg #1B77F2)

artifacts/bpolytix/app/contact/page.tsx
- Add class "glow-border" to the form container
- Add class "glow-border-blue" to the form submit button

artifacts/bpolytix/app/website-in-3-days/page.tsx
- Add class "glow-border" to the features/pricing card blocks
- Add class "glow-border-blue" to the hero CTA and form submit button

artifacts/bpolytix/app/page.tsx (homepage)
- Add class "glow-border" to every card in the services bento grid
- Add class "glow-border" to the trust block cards (if they are card elements)
- Add class "glow-border-blue" to the primary hero CTA button

---

PART 4 — SELF-TEST BEFORE COMMITTING

Before committing, do the following and report the result:

1. Run the dev server: cd artifacts/bpolytix && npm run dev
2. Open http://localhost:3000 in the browser preview
3. Scroll down the homepage — confirm section reveals are visibly distinct (not just a faint fade)
4. Hover over a service card — confirm it lifts AND shows a teal border glow
5. Hover over the primary CTA button — confirm blue glow appears
6. Check the pricing page — confirm card hover works on plan cards and FAQ cards
7. If any effect is not visible, fix it before committing

Report the self-test result in your completion summary.

---

DO NOT:
- Change any copy or page layout
- Touch layout.tsx
- Touch the hero pulse dot
- Install any new packages
- Add any animation type not listed above
- Modify the Resend API routes or any backend files

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Copy everything between the lines and paste into Replit Agent.