━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
REPLIT AGENT PROMPT — SESSION 4: INTEGRATIONS
Session: 4 | Phase: 1 | Date: 2026-04-22
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You are building bpolytix.com — a professional SaaS/BPO marketing website.

BEFORE DOING ANYTHING ELSE, read and internalise these rules:

TECH STACK (do not deviate):
- Framework: Next.js 15 with App Router
- Styling: Tailwind CSS
- Animations: Framer Motion
- Icons: Lucide React ONLY — no other icon libraries
- Fonts: Syne (headings) + DM Sans (body) via next/font/google
- No other packages without asking first

COLOURS (exact hex — use only these, no others):
- Background:  #0A0F1A
- Surface:     #0F1622
- Accent:      #1B77F2  (ONE CTA button + key icons only)
- Highlight:   #00D4AA  (stats and hover states only)
- Text:        #F5F7FA
- Muted:       #8892A4
- Border:      rgba(255,255,255,0.08)

PROJECT ROOT: All files are inside artifacts/bpolytix/ — this is the Next.js project root.
All file paths below are relative to artifacts/bpolytix/.

OUTPUT RULES:
- Build ONLY what is in TODAY'S TASK — three integrations, in order
- Do not modify any page layouts, copy, or visual design
- Do not install packages outside the tech stack
- After completing each integration, confirm what was built and what files changed
- If you hit an error you cannot fix in 2 attempts, stop and describe it clearly

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TODAY'S TASK: Three integrations in this order. Complete and verify each before starting the next.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTEGRATION 1 — Resend contact form
─────────────────────────────────────
What it does: Sends contact form submissions to info@bpolytix.com via Resend API.

Environment variable needed:
- RESEND_API_KEY — prompt the user to add this to Replit Secrets before proceeding

Implementation steps:

STEP 1 — Install Resend SDK:
npm install resend

STEP 2 — Create the API route at: app/api/contact/route.ts
The route must:
- Accept POST requests with fields: name, company, email, service, message
- Use Resend to send an email to info@bpolytix.com
- Send FROM: onboarding@resend.dev
- Subject line: "New enquiry from [name] — [company]"
- Body: plain text listing all submitted fields clearly
- Return JSON { success: true } on success
- Return JSON { error: "message" } with status 500 on failure

STEP 3 — Update the contact form at: app/contact/page.tsx
The current handleSubmit function uses a mailto: fallback. REPLACE IT ENTIRELY with this logic:
- Remove the window.location.href mailto line and the setTimeout success call
- Replace with a fetch POST to /api/contact sending { name, company, email, service, message } as JSON
- On response { success: true }: call setState("success")
- On failure or non-ok response: call setState("error")
- The submit button must be disabled and show "Sending…" while the request is in flight
- Do not change any other part of the form — only replace the handleSubmit function body

The existing form fields, state machine (idle/submitting/success/error), and UI are already correct — do not touch them. Only replace the submission logic inside handleSubmit.

─────────────────────────────────────
INTEGRATION 2 — Tawk.to live chat widget
─────────────────────────────────────
What it does: Adds a live chat widget to every page.

Credentials (hardcode these — no env var needed):
- Property ID: 69e83a5ccbb7811c31e4d217
- Widget ID: 1jmpi7ura
- Full embed src: https://embed.tawk.to/69e83a5ccbb7811c31e4d217/1jmpi7ura

Implementation steps:

STEP 1 — Create: components/TawkChat.tsx
- Add "use client" at the top
- Use useEffect to inject the Tawk.to script into the document, matching this exact script:

var Tawk_API=Tawk_API||{}, Tawk_LoadStart=new Date();
(function(){
var s1=document.createElement("script"),s0=document.getElementsByTagName("script")[0];
s1.async=true;
s1.src='https://embed.tawk.to/69e83a5ccbb7811c31e4d217/1jmpi7ura';
s1.charset='UTF-8';
s1.setAttribute('crossorigin','*');
s0.parentNode.insertBefore(s1,s0);
})();

- The useEffect must have an empty dependency array [] so it runs once on mount
- The component returns null (no visible UI)

STEP 2 — Edit: app/layout.tsx
- Import TawkChat at the top
- Render <TawkChat /> just before the closing </body> tag
- Do not change anything else in layout.tsx — fonts, metadata, and html/body structure must remain exactly as they are

─────────────────────────────────────
INTEGRATION 3 — WhatsApp floating button
─────────────────────────────────────
What it does: A fixed floating button on all pages that opens a WhatsApp chat.

WhatsApp URL: https://wa.me/27781790363?text=Hi%2C+I%27d+like+to+find+out+more+about+BPOLytix

Implementation steps:

STEP 1 — Create: components/WhatsAppButton.tsx
- Add "use client" at the top
- Fixed position: bottom-right, 24px from bottom, 24px from right
- z-index: 50
- Shape: circle, 56px × 56px
- Background: #25D366 (WhatsApp green — intentional exception to colour rules)
- Icon: Lucide React MessageCircle, white, 24px
- On click: open WhatsApp URL in a new tab
- On hover: scale to 1.05, transition 150ms ease
- Tooltip on hover: "Chat on WhatsApp" — small label appearing above the button

STEP 2 — Edit: app/layout.tsx
- Import WhatsAppButton at the top
- Render <WhatsAppButton /> just before the closing </body> tag, alongside <TawkChat />
- Do not change anything else in layout.tsx

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
After all three integrations are complete, confirm:
1. Exact files created or modified
2. Whether RESEND_API_KEY was added to Replit Secrets
3. Any errors encountered
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━