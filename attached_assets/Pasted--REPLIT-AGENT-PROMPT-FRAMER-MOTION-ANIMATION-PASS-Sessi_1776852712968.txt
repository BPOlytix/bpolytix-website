━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
REPLIT AGENT PROMPT — FRAMER MOTION ANIMATION PASS
Session: 6 | Phase: 2 | Date: 2026-04-22
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You are building bpolytix.com — a professional SaaS/BPO marketing website.

BEFORE DOING ANYTHING ELSE, read and internalise these rules:

TECH STACK (do not deviate):
- Framework: Next.js 15 with App Router
- Styling: Tailwind CSS
- Animations: Framer Motion
- Icons: Lucide React ONLY — no other icon libraries
- Fonts: Syne (headings) + DM Sans (body) via next/font/google
- No other packages without asking first

COLOURS (exact hex — use only these, no others):
- Background:  #0D1B2A
- Surface:     #111F2E
- Accent:      #1B77F2  (ONE CTA button + key icons only)
- Highlight:   #00D4AA  (stats and hover states only)
- Text:        #F5F7FA
- Muted:       #8892A4
- Border:      #1E2D3D

TYPOGRAPHY:
- Import Syne and DM Sans via next/font/google in layout.tsx
- Hero headline: min 72px, font-weight 700, line-height 1.05
- Section headlines: 40–52px, line-height 1.1
- Body: 16–18px, line-height 1.7
- NEVER use Inter, Roboto, Arial, or system-ui for headings

LAYOUT RULES:
- Dark throughout — #0D1B2A background on ALL sections
- No alternating white/grey sections
- Section padding: minimum 80px vertical, preferred 100–120px
- NO: stock photos, blob illustrations, gradient backgrounds
- YES: SVG flow diagrams, code-style callouts, large real numbers, bento grids

ANIMATION RULES (Framer Motion):
- Scroll-triggered ONLY — nothing animates on page load
- Use useInView from Framer Motion with { once: true } for all scroll triggers
- Add "use client" directive to any component that uses Framer Motion hooks
- Stat numbers: count-up animation triggered once on scroll into view
- Cards: translateY(-4px) on hover, 200ms ease — no bounce, no scale, no rotation
- CTA button: box-shadow glow pulse on hover only — not on idle
- Section reveals: opacity 0→1, y 20→0, triggered on scroll
- Hero pulse dot: already implemented — do NOT touch it
- DO NOT animate anything on page load
- DO NOT add any effect not listed above

COPY RULES:
- Do not change any existing copy
- Do not add, remove, or rewrite any text

QUALITY CHECK (run before presenting result):
- All animations are scroll-triggered (none fire on page load)
- No bounce, scale, or rotation effects anywhere
- Fonts remain Syne + DM Sans
- Background remains #0D1B2A throughout
- No layout or copy changes made

OUTPUT RULES:
- Build ONLY what is in TODAY'S TASK
- Do not add unrequested features
- After completing, list exactly what was built and what files were changed
- If you hit an error you cannot fix in 2 attempts, stop and describe it clearly

---

TODAY'S TASK: Add Framer Motion scroll-triggered animations to all existing pages.

The Next.js project lives at: artifacts/bpolytix/

PAGES TO ANIMATE (in this order):
1. Homepage — artifacts/bpolytix/app/page.tsx (and any component files it imports)
2. Services page — artifacts/bpolytix/app/services/page.tsx
3. Pricing page — artifacts/bpolytix/app/pricing/page.tsx
4. Contact page — artifacts/bpolytix/app/contact/page.tsx
5. Website in 3 Days page — artifacts/bpolytix/app/website-in-3-days/page.tsx

ANIMATION REQUIREMENTS PER ELEMENT TYPE:

Section wrappers (every major section):
- opacity: 0 → 1, y: 20 → 0
- duration: 0.6s, ease: easeOut
- triggered by useInView, once: true

Cards and bento grid items (Services section, Pricing cards):
- Same fade-in as sections above, but staggered — each card delays by 0.1s after the previous
- On hover: translateY(-4px), transition 200ms ease — apply via Tailwind or inline style

Stat numbers (Stats bar on homepage):
- Count-up from 0 to final value, triggered once on scroll into view
- Duration: 1.5s, easeOut
- Stats to animate: 70 (%), 0 (R0 — do not count, just reveal), 12 (months), 30 (days)

CTA buttons (all primary blue buttons with bg #1B77F2):
- On hover only: add box-shadow glow — rgba(27, 119, 242, 0.4), blur 16px, spread 0
- No animation at idle
- Apply via Framer Motion whileHover

DO NOT:
- Modify any copy or layout
- Touch layout.tsx
- Touch the hero pulse dot — it is already implemented
- Install any new packages (Framer Motion is already installed)
- Add any animation type not listed above
- Animate anything on page load

After completing, list:
1. Every file that was modified
2. Every animation type that was added and where
3. Any components that required "use client" directive to be added

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Copy everything between the lines and paste into Replit Agent.