━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
REPLIT AGENT PROMPT — CALCULATOR QUOTING ENGINE (FULL REBUILD)
Session: 7 | Phase: 2c | Date: 2026-04-22
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You are building bpolytix.com — a professional SaaS/BPO marketing website.
The Next.js project lives at: artifacts/bpolytix/
All files must be created or modified inside this directory.

CONTEXT:
The existing calculator at app/calculator/page.tsx is being replaced
with a quoting engine. The calculator logic in lib/calculatorLogic.ts
is correct — do NOT modify it. Recharts is already installed.
All existing components in components/calculator/ remain — reuse them.

DO NOT touch:
- lib/calculatorLogic.ts
- components/calculator/Stepper.tsx
- components/calculator/ToggleSwitch.tsx
- components/calculator/PillSelect.tsx
- components/calculator/MetricTile.tsx
- components/calculator/CostBreakdown.tsx
- components/calculator/DotGrid.tsx
- components/calculator/ResultsCharts.tsx
- components/calculator/ScreenshotCard.tsx

Also run this cleanup in the Replit shell:
  rm -rf attached_assets/
  echo "attached_assets/" >> .gitignore

COLOURS (exact hex — use only these):
- Background:  #0D1B2A
- Surface:     #111F2E
- Card:        #1C2A3A
- Accent:      #1B77F2
- Highlight:   #00D4AA
- Text:        #F5F7FA
- Muted:       #8892A4
- Border:      #1E2D3D
- Success:     #00D4AA
- Danger:      #FF4444

DESIGN RULES:
- Dark throughout — no white or grey backgrounds
- Fonts: Syne headings, DM Sans body
- Mobile-first — fully responsive at 375px
- Side panels: desktop only (hidden below lg breakpoint)
- Lucide React icons only

OUTPUT RULES:
- Build ONLY what is listed below
- List every file created or modified when done
- If an error cannot be fixed in 2 attempts, stop and describe it clearly

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE OVERVIEW — READ BEFORE BUILDING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The calculator is now a quoting engine with this flow:

1. MINIMAL HERO — compressed, dot grid, headline only
2. STEP 1 — Service selector (pill tabs, one at a time, discrete)
3. STEP 2 — Industry selector (dropdown, affects copy only)
4. ACTIVE CALCULATOR — inputs + breakdown for selected service
5. PROMPT BAR — appears after calculation, offers "Add another" or "Build my quote"
6. QUOTE SUMMARY CARD — full quote with ref number, all completed services
7. THREE-COLUMN LAYOUT — left drill-down | centre charts + inputs | right inclusions
8. LEAD CAPTURE — email field, WhatsApp CTA

Services (5 discrete tabs):
- Bookkeeping & Accounting
- CFO-as-a-Service
- AI Automation
- Web App Development
- Android App Development

Industries (8, copy only):
Professional Services | Retail & E-commerce | Construction & Property |
Logistics & Transport | Healthcare & Wellness | Tech & Startups |
Hospitality & Tourism | Non-profit & NGO

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BUILD INSTRUCTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TASK 1 — QUOTE REFERENCE GENERATOR
Create artifacts/bpolytix/lib/quoteRef.ts

Export a function generateQuoteRef():
- Format: BPQ-YYYY-XXXX
- YYYY = current year
- XXXX = random 4-digit number padded with zeros
- Example: BPQ-2026-0042
- Store in sessionStorage so it persists across tab switches
  but resets on new page load

━━━

TASK 2 — INDUSTRY COPY MAP
Create artifacts/bpolytix/lib/industryCopy.ts

Export an object INDUSTRY_COPY mapping each industry to:
{
  reinvestLine: string  // how savings are typically reinvested in this sector
  roiMultiplier: number // conservative ROI multiplier for reinvestment (3-6x)
  sectorNote: string   // one sentence about BPO relevance to this industry
}

Values:
Professional Services:
  reinvestLine: "Reinvested into client acquisition and service expansion"
  roiMultiplier: 5
  sectorNote: "Professional service firms typically save 35–55% by outsourcing non-billable finance functions."

Retail & E-commerce:
  reinvestLine: "Reinvested into inventory, digital marketing and fulfilment"
  roiMultiplier: 4
  sectorNote: "Retail SMEs reduce overhead 30–45% by outsourcing bookkeeping and compliance."

Construction & Property:
  reinvestLine: "Reinvested into project working capital and equipment"
  roiMultiplier: 4
  sectorNote: "Construction firms save significantly on project accounting and compliance overhead."

Logistics & Transport:
  reinvestLine: "Reinvested into fleet, route optimisation and driver costs"
  roiMultiplier: 4
  sectorNote: "Transport businesses reduce admin overhead 30–40% through finance outsourcing."

Healthcare & Wellness:
  reinvestLine: "Reinvested into patient experience, equipment and digital ops"
  roiMultiplier: 5
  sectorNote: "Healthcare practices save 40–55% by outsourcing billing, compliance and reporting."

Tech & Startups:
  reinvestLine: "Reinvested into product development and growth marketing"
  roiMultiplier: 6
  sectorNote: "Startups that outsource finance functions early scale 2x faster on average."

Hospitality & Tourism:
  reinvestLine: "Reinvested into guest experience, marketing and seasonal ops"
  roiMultiplier: 3
  sectorNote: "Hospitality operators save 25–40% by outsourcing back-office finance functions."

Non-profit & NGO:
  reinvestLine: "Reinvested into programme delivery and donor reporting"
  roiMultiplier: 3
  sectorNote: "NGOs improve fund utilisation by 30–45% through outsourced finance and compliance."

━━━

TASK 3 — REBUILD app/calculator/page.tsx

Replace the entire file. This is a full rewrite.
Use "use client" at the top.

STATE VARIABLES NEEDED:
- activeService: string (default 'bookkeeping')
- industry: string (default 'Professional Services')
- completedServices: Record<string, ServiceResult> — stores results per service
- quoteRef: string — generated on mount from generateQuoteRef()
- showQuote: boolean (default false) — shows full quote card
- currency: 'ZAR' | 'GBP' (default 'ZAR')

SERVICE IDs:
'bookkeeping' | 'cfo' | 'automation' | 'webapp' | 'android'

━━━
SECTION A — MINIMAL HERO

Height: max 220px desktop, 160px mobile
Structure:
- DotGrid component fills the section (absolute, inset 0)
- Content centered, position relative z-index 1
- Anchor label: "COST CALCULATOR" — DM Sans 12px #8892A4
  uppercase letter-spacing 0.1em
- Headline: "Calculate your true in-house cost."
  Syne 42px desktop / 28px mobile, #F5F7FA, weight 700
  line-height 1.05, letter-spacing -0.022em
- No subheadline — remove it entirely
- No CTA button
- Framer Motion: headline y 20→0, opacity 0→1, 0.5s on load

━━━
SECTION B — CONFIGURATOR CARD

Card: background #111F2E, border #1E2D3D, border-radius 16px,
padding 24px desktop / 16px mobile
Max-width 1440px, centered, 32px horizontal padding

TWO-ROW LAYOUT inside card:

Row 1 — Service selector:
- Label (DM Sans 12px #8892A4 uppercase): "01 — Select a service"
- Pill row of 5 service pills:
  - Default: background #1C2A3A, text #8892A4, border #1E2D3D
  - Active: background #1B77F2, text #F5F7FA, border transparent
  - Completed (in completedServices): background rgba(0,212,170,0.15),
    text #00D4AA, border rgba(0,212,170,0.3)
  - Completed pill shows a small checkmark icon (Lucide Check, 12px)
    before the service name
  - All pills border-radius 9999px, DM Sans 13px, padding 8px 16px
  - Lucide icons before each label (same as before)
  - Mobile: horizontally scrollable row, no wrap

Row 2 — Industry + Currency:
- Left side: Label "02 — Your industry" + select dropdown
  - Dropdown: background #1C2A3A, border #1E2D3D, text #F5F7FA,
    border-radius 8px, DM Sans 13px, padding 10px 14px
  - Options: all 8 industries
- Right side: ZAR/GBP currency toggle (existing style)
- Layout: flex row, space-between, gap 16px
- Mobile: stack vertically

━━━
SECTION C — THREE-COLUMN LAYOUT

Only visible when activeService is set.
Max-width 1440px centered.

Desktop: CSS grid — 260px | 1fr | 260px, gap 20px
Mobile: single column, side panels hidden (display none below lg)

LEFT PANEL — "Your true in-house cost"
Create artifacts/bpolytix/components/calculator/DrillDownPanel.tsx

Props: { result: ServiceResult, currency: 'ZAR' | 'GBP', service: string }

Panel: background #111F2E, border #1E2D3D, border-radius 16px,
padding 20px, position sticky, top 24px

Title: "YOUR TRUE IN-HOUSE COST" — DM Sans 12px #8892A4 uppercase
letter-spacing 0.08em, margin-bottom 12px

Three accordion sections using Framer Motion AnimatePresence:

Each accordion:
- Header: flex space-between, padding 10px 12px,
  background #1C2A3A, border-radius 8px, cursor pointer
- Header text: DM Sans 13px #F5F7FA font-weight 500
- Chevron: Lucide ChevronDown, rotates 180° when open,
  transition 0.2s
- Body: AnimatePresence height 0→auto, opacity 0→1, 0.25s
- Body background: #111F2E, padding 10px 12px
- All sections open by default

ACCORDION 1 — "Staff cost"
Line items (each as flex space-between):
- Base salary + ⓘ icon
- Employer UIF (1%) + ⓘ icon
- Employer SDL (1%) + ⓘ icon
- Subtotal row (bold)

ACCORDION 2 — "Infrastructure"
Line items:
- Workspace / hot desk + ⓘ icon
- Software stack + ⓘ icon (shows Xero + M365 breakdown)
- Device amortisation + ⓘ icon
- Subtotal row (bold)

ACCORDION 3 — "Hidden costs"
Line items:
- Recruitment & onboarding + ⓘ icon
- Management overhead + ⓘ icon
- Subtotal row (bold)

ⓘ TOOLTIP SPEC:
- Lucide Info icon, 12px, #8892A4
- On hover: tooltip div appears above icon
  position absolute, z-index 50
  background #1C2A3A, border #1E2D3D, border-radius 8px
  padding 8px 10px, DM Sans 11px #8892A4
  max-width 200px, pointer-events none
- Tooltip text per line item:
  Base salary (bookkeeping): "Indeed SA benchmark — R15,698/mo (Apr 2025)"
  Base salary (accountant): "Indeed SA benchmark — R24,748/mo (Apr 2025)"
  Base salary (CFO): "User-entered current FM/CFO salary"
  Base salary (automation): "VA benchmark — R7,525/mo loaded to R105/hr"
  Base salary (webapp): "Indeed SA mid developer — R17,791/mo (Apr 2025)"
  Base salary (android): "Indeed SA mobile dev — R43,946/mo (Apr 2025)"
  UIF: "SARS mandatory — 1% employer contribution"
  SDL: "SARS mandatory — 1% if annual payroll exceeds R500,000"
  Workspace: "Cape Town coworking benchmark — Regus/WeWork 2025"
  Software: "Xero Standard R795 + Microsoft 365 Basic R112/mo"
  Device: "Laptop amortised over 36 months"
  Recruitment: "SARA benchmark — 8% of annual salary (admin/finance)"
  Management: "Industry standard — 10% of monthly salary cost"

PANEL TOTAL BOX:
background #1C2A3A, border #1E2D3D, border-radius 10px,
padding 12px, margin-top 12px
Label: "Total true monthly cost" DM Sans 12px #8892A4
Value: Syne 20px #FF4444 (red — signals cost)
Show value from result.inHouseMonthly (or inHouseTotal for project tabs)

LINE ITEM STYLE:
- flex space-between, padding 5px 0
- border-bottom 0.5px #1E2D3D (last item no border)
- Label: DM Sans 12px #8892A4 + inline ⓘ icon
- Value: DM Sans 12px #F5F7FA font-weight 500
- All values formatted with R prefix (or £ if GBP)
  No decimals — Math.round() all values

CENTRE COLUMN
Contains (top to bottom):

1. QUOTE SUMMARY CARD (always visible once first service calculated)
   Reuse and update ScreenshotCard.tsx OR build inline — either is fine.
   
   Card: background #111F2E, border #1E2D3D, border-radius 16px,
   padding 24px

   Header row:
   - Left: "BPOLytix" Syne 16px #F5F7FA weight 700
     + "Cost Analysis & Quote" DM Sans 12px #8892A4
   - Right: quote ref (DM Sans 11px #8892A4) + date + industry
     (DM Sans 11px #1B77F2)

   Per-service rows (one per completedServices entry + active service):
   - Service name: DM Sans 14px #F5F7FA weight 500
   - Detail line: "In-house: R24,783 | BPOLytix: R8,500/mo"
     DM Sans 12px #8892A4
   - Right: saving value #00D4AA Syne 16px + saving % DM Sans 11px #8892A4
   - Border-bottom #1E2D3D between rows

   Total row:
   - "Total annual saving" Syne 16px #F5F7FA
   - Value: Syne 32px #00D4AA — the headline number
   - Framer Motion: count-up animation 1.2s on value change

   Reinvestment box:
   - background rgba(27,119,242,0.1), border rgba(27,119,242,0.2)
   - border-radius 10px, padding 12px
   - Line 1 (DM Sans 13px #8892A4): industryCopy[industry].reinvestLine
   - Line 2 (DM Sans 14px #1B77F2 weight 500):
     "R[annualSaving] → potential R[annualSaving × roiMultiplier]
     revenue impact in Year 1"
   - Line 3 (DM Sans 11px #8892A4 italic):
     industryCopy[industry].sectorNote

   Compounding note:
   "Compounding advantage: save → reinvest → grow → repeat"
   DM Sans 12px #8892A4 italic, centered, dashed border #1E2D3D,
   border-radius 8px, padding 6px, margin 10px 0

   Motto row (flex space-between):
   - "No invoice until you're satisfied." DM Sans 12px #8892A4
   - "You own it after 12 months." DM Sans 12px #8892A4

   CTA row (two buttons):
   - "Email me this quote →" — primary pill #1B77F2 Syne 13px
   - "WhatsApp this result →" — ghost pill, border #1E2D3D

   WhatsApp CTA pre-fills this message:
   "Hi BPOLytix, my quote ref is [quoteRef]. My estimated annual
   saving is R[totalAnnualSaving]. Industry: [industry].
   I'd like to discuss further."
   URL: https://wa.me/27781790363?text=[encoded message]

2. METRIC TILES (3 tiles, existing style)
   Monthly saving | Annual saving | Saving %
   Transparency note below:
   "Full loaded cost basis. Core salary differential alone: [X]%."
   Where X = (base_salary - bpolytix_fee) / base_salary * 100, rounded

3. RECHARTS PANELS (existing ResultsCharts component)
   Pass resultsToShow and combinedToShow as before

4. ACTIVE CALCULATOR INPUTS
   Existing tab inputs for activeService
   Existing CostBreakdown component
   BPOLytix fee display line

5. PROMPT BAR
   Only visible when activeService has a valid result
   background rgba(0,212,170,0.08), border rgba(0,212,170,0.2)
   border-radius 12px, padding 12px 16px
   flex space-between, align-items center

   Left text (DM Sans 13px #00D4AA weight 500):
   "[Service name] calculated. Add another service or build your quote."

   Right buttons:
   - "+ Add another service" — ghost pill, border #1E2D3D, #8892A4
     On click: saves current result to completedServices,
     focuses next uncompleted service pill
   - "Build my full quote →" — pill #1B77F2, Syne 13px
     On click: saves current result, sets showQuote true,
     scrolls to quote summary card

   Framer Motion: bar slides up (y 16→0, opacity 0→1, 0.4s)
   when result becomes valid

RIGHT PANEL — "What BPOLytix includes"
Create artifacts/bpolytix/components/calculator/InclusionsPanel.tsx

Props: { service: string, bpolytixFee: number, currency: string }

Same panel style as left panel.

Title: "WHAT BPOLYTIX INCLUDES"

TWO accordions:

ACCORDION 1 — "What you get"
Content varies by service:

bookkeeping:
✓ Dedicated finance specialist
✓ Xero licence included
✓ VAT returns
✓ Monthly reconciliation
✓ SARS compliance support
✓ Management accounts (if selected)

cfo:
✓ Fractional CFO / Financial Manager
✓ Monthly management accounts
✓ Board reporting (if selected)
✓ Cash flow forecasting
✓ SARS and regulatory compliance
✓ Investor/fundraising support (if selected)

automation:
✓ Custom workflow design
✓ Browser automation build
✓ Multi-platform integration
✓ Human review layer (if selected)
✓ Monthly maintenance
✓ Error monitoring and alerts

webapp:
✓ Full-stack development team
✓ UI/UX design included
✓ QA and testing
✓ Deployment and hosting setup
✓ 30-day post-launch support
✓ Integration development (if selected)

android:
✓ Android development team
✓ UI/UX design included
✓ Google Play submission
✓ QA and device testing
✓ 30-day post-launch support
✓ Backend/API integration (if selected)

ACCORDION 2 — "What you don't pay for"
(Same for all services):
✗ UIF / SDL contributions
✗ Office / desk cost
✗ Software licences
✗ Recruitment fees
✗ Leave cover
✗ Device / equipment
✗ Management overhead

PANEL TOTAL BOX:
Label: "BPOLytix monthly fee" (or "BPOLytix project fee" for webapp/android)
Value: Syne 20px #1B77F2

MOTTO BOX below total:
background rgba(0,212,170,0.08), border-radius 10px, padding 10px
"No invoice until you're satisfied." DM Sans 12px #00D4AA weight 500
"You own it after 12 months." DM Sans 12px #00D4AA

━━━
SECTION D — LEAD CAPTURE (Email quote)

When "Email me this quote →" is clicked, show an inline form
directly below the quote summary card (no modal):

Fields:
- First name (text input)
- Email address (text input)
- Company name (text input, optional)

Submit button: "Send my quote →" #1B77F2 pill Syne 13px

On submit:
1. POST to /api/quote-email route (create this route)
2. Route sends two emails via Resend:
   a. To mitesh@bpolytix.com:
      Subject: "New calculator quote — [quoteRef] — [industry]"
      Body: name, email, company, industry, quoteRef, date,
      per-service breakdown, total annual saving
   b. To prospect (their email):
      Subject: "Your BPOLytix cost analysis — [quoteRef]"
      Body: branded summary of their quote —
      same content as the quote card, formatted as email
      Sign-off: "We'll be in touch within 1 business day.
      No invoice until you're satisfied."
3. On success: replace form with:
   "Your quote has been sent to [email]. Quote ref: [quoteRef].
   We'll be in touch within 1 business day."
   DM Sans 14px #00D4AA

━━━
SECTION E — DISCLAIMER

DM Sans 12px #8892A4 centered, margin-top 24px:
"Estimates based on published SA salary benchmarks (Indeed/PayScale 2025),
SARS statutory rates, and BPOLytix standard pricing.
Actual costs vary by scope. Contact us for a tailored quote."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ALSO CREATE — API ROUTE

File: artifacts/bpolytix/app/api/quote-email/route.ts

POST handler using Resend (RESEND_API_KEY already in env):
- Parse body: { name, email, company, industry, quoteRef,
  date, services[], totalAnnualSaving, currency }
- Send email to mitesh@bpolytix.com (admin notification)
- Send email to prospect (their quote)
- Both from: no-reply@bpolytix.com
- Return { success: true } on success

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHEN COMPLETE — VERIFY:
- [ ] /calculator loads with minimal hero (dot grid visible)
- [ ] Service pills work — one active at a time
- [ ] Completed services show green ✓ pill
- [ ] Industry dropdown changes reinvestment copy
- [ ] Quote ref auto-generated on load (BPQ-YYYY-XXXX)
- [ ] Quote summary card shows correct per-service rows
- [ ] Prompt bar appears after calculation with two buttons
- [ ] "Add another service" saves result and switches tab
- [ ] "Build my full quote" scrolls to quote card
- [ ] Left drill-down panel shows accordions with tooltips
- [ ] Right inclusions panel shows correct items per service
- [ ] Email form submits and sends two emails via Resend
- [ ] Mobile: side panels hidden, single column layout
- [ ] No white backgrounds anywhere
- [ ] attached_assets/ removed from repo

List every file created or modified when done.
Do not proceed further — wait for instruction.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━